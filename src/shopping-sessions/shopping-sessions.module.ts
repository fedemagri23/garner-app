import { Module } from '@nestjs/common';

/**
 * Phase 3 — active trip, actual purchase prices, running total, completion.
 *
 * Registered from the foundation so the boundary exists before the behaviour
 * does; its four layers (domain / application / infrastructure / presentation)
 * are filled in by that phase.
 */
@Module({})
export class ShoppingSessionsModule {}
